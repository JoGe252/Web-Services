version: '3.9'

services:
  mongo:
    image: mongo:latest
    container_name: inventory_mongo
    ports:
      - "27017:27017"

  api:
    build: .
    container_name: inventory_api
    ports:
      - "8000:8000"
    depends_on:
      - mongo